{
  "name": "luxe-en-vogue-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Requires PAYSTACK_SECRET_KEY (and optionally PUBLIC_BASE_URL) to be set as environment variables in the Vercel project settings. See /api/create-checkout-session.js for details. No extra dependencies needed — uses Node 18+ native fetch.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}